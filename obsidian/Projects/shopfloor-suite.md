---
project: shopfloor-suite
tags: [ai-memory, project-context]
---

# Project Context: shopfloor-suite

## Key Lessons & Facts
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:12
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:13
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:14
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:15
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:16
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:17
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:18
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:19
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:20
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:21
- UtcTimeService.utcToLocal() relies on browser/OS timezone rather than backend APP_TIMEZONE; use Intl.DateTimeFormat with an explicit timeZone option to force a fixed timezone.
  - Recorded: 2026-09-01 13:46:22

